{
  "name": "@altcontext/backend",
  "version": "1.0.0",
  "description": "Backend email capture and marketing intelligence service",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prestart": "npm run build",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "tsx --test --test-concurrency=1 test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier . --check",
    "format:fix": "prettier . --write",
    "check": "npm run typecheck && npm run lint && npm run format",
    "dead-exports": "sh -c 'output=$(ts-prune | grep -v \"(used in module)\" || true); if [ -n \"$output\" ]; then echo \"$output\"; exit 1; fi'",
    "duplicates": "jscpd src/ --min-tokens 50 --min-lines 6",
    "prisma:generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "migrate:reset": "prisma migrate reset --force",
    "db:studio": "prisma studio",
    "retention:purge": "tsx src/scripts/purge-retention.ts",
    "rollups:run": "tsx src/scripts/rollups-run.ts",
    "rollups:backfill": "tsx src/scripts/rollups-backfill.ts",
    "rollups:status": "tsx src/scripts/rollups-status.ts",
    "rollups:discrepancy": "tsx src/scripts/rollups-discrepancy.ts",
    "rollups:mv:init": "tsx src/scripts/rollups-mv-init.ts",
    "rollups:mv:refresh": "tsx src/scripts/rollups-mv-refresh.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/formbody": "^8.0.2",
    "@fastify/middie": "^9.1.0",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/static": "^9.0.0",
    "dotenv": "^17.3.1",
    "fastify": "^5.7.4",
    "pg": "^8.18.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@prisma/adapter-pg": "^7.4.0",
    "@prisma/client": "^7.4.0",
    "@types/node": "^25.2.3",
    "@types/pg": "^8.16.0",
    "@typescript-eslint/eslint-plugin": "^8.55.0",
    "@typescript-eslint/parser": "^8.55.0",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "jscpd": "^4.0.8",
    "prettier": "^3.8.1",
    "prisma": "^7.4.0",
    "ts-prune": "^0.10.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
